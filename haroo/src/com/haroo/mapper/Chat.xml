<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.haroo.mapper.ChatMapper">
	<cache />
	
	<select id="insertChat" parameterType="ChatVO">
		insert into chat(
		chatNo, chatName, chatTime, chatFrom, chatTo
		)values(
		co_seq.nextval, #{chatName}, #{chatTime}, #{chatFrom}, #{chatTo}
		)

	</select>
	
	<insert id="uploadFile" parameterType="FileVO">
		insert into contents(
		co_no, co_name)
			values(
			co_seq.nextval, #{co_name})
	</insert>
	
	<select id="listFile"  resultType="FileVO">
		select * from contents
		<!-- <if test="area != null">
			<where>
				where (title LIKE %JSP% OR writer LIKE %JSP)
				<foreach collection="area" item="item" separator="OR"
					open="(" close=")">
					${item} LIKE #{searchKey}
				</foreach>
			</where>
		</if> -->
		order by co_no desc
	</select>
</mapper>