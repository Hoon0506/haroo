<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.haroo.mappeer.ChatMapper">
	<cache />
	
	<insert id="insertChat" parameterType="Chat">
		insert into chat(
		seq, chatName, chatTime, chatFrom, chatTo
		)values(
		ch_seq.nextval, #{chatName}, #{chatTime}, #{chatFrom}, #{chatTo}
		)

	</insert>
	
</mapper>