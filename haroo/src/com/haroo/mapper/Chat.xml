<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.haroo.mappeer.ChatMapper">
	<cache />
	
	<select id="insertChat" parameterType="Chat">
		insert into chat(
		chatNo, chatName, chatTime, chatFrom, chatTo
		)values(
		ch_seq.nextval, #{chatName}, #{chatTime}, #{chatFrom}, #{chatTo}
		)

	</select>
	
</mapper>